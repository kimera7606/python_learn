'''
crearemos el entorno y modulos necesarios
creamos una carpeta en la cual estara nuestro proyecto
empezemos primero creando nuestro entorno virtual
1) python3 -m venv venv
2)una vez creado dbeemos activarlo con : source venv/bin/activate
*("recuerda que lo puedes desactivar con el comando deactivate")

ahora viene la instalacion de los modulos:
1) actualizamos nuestro pip con el comando : python3 .m pip install --unpgrade pip
2) pip install fastapi
3) pip install uvicorn

'''